spring:
  data:
    mongodb:
      uri: mongodb://root:sp77shtq@dbconn.sealosbja.site:30428/agent_platform?authSource=admin&directConnection=true
  application:
    name:
      seeklight-backend
  datasource:
    url: jdbc:mysql://root:s7756zxw@dbconn.sealosbja.site:33438/agent_platform_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2b8
    username: root
    password: s7756zxw
    hikari:
      connection-test-query: SELECT 1
      connection-timeout: 60000
      idle-timeout: 500000
      max-lifetime: 540000
      maximum-pool-size: 12
      minimum-idle: 10
      pool-name: SPHHikariPool
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: is_deleted # 全局逻辑删除字段名
      logic-delete-value: 1 # 逻辑已删除值。可选，默认值为 1
      logic-not-delete-value: 0 # 逻辑未删除值。可选，默认值为 0
